# OR-CODE-GENERATOR
Mini Project 
# 🔲 QR Code Generator

This is a simple Python-based **QR Code Generator** that converts any text, URL, or data into a QR code image using the `qrcode` and `Pillow` libraries.

---

## 🚀 Features

- ✅ Generate QR code from any input (text, URL, etc.)
- 💾 Save the generated QR code as an image file (`.png`)
- 🧠 Simple and easy to use via command-line
- ⚙️ Minimal dependencies and fast execution

---
